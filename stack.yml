version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:31112
functions:
  openclojure:
    lang: dockerfile
    handler: ./openclojure
    image: openclojure:latest
    environment:
        read_timeout: 60s
        write_timeout: 60s
        upstream_timeout: 55s
